What happens when you initialize a repository? Why do you need to do it?
A new directory named .git is created. I need to do this to create the environment for tracking my files

How is the staging area different from the working directory and the repository? What value do you think it offers?
The staging area only contains the files which I want to commit. It separate what I want to keep trace changes of.

How can you use the staging area to make sure you have one commit per logical change?
Only add files that have same logical changes

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
When we want to test something. It helps in the possibility to go back to initial state of the files when have modified.

How do the diagrams help you visualize the branch structure?
It show us the different branches

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
The different changes made to the files are merged together. Because all the commit done yet are made on a single branch

What are the pros and cons of Git's automatic merging vs. always doing merges manually?
The pros: rapidity, cons: it can be some times tricky to determine which modification is the right one
